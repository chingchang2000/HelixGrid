name: helixgrid

services:
  coordinator:
    build:
      context: ./coordinator
    environment:
      HELIX_ADDR: 0.0.0.0:8080
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "/helixd"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 2s
    restart: unless-stopped

  worker:
    build:
      context: ./worker
    environment:
      HELIX_COORDINATOR: http://coordinator:8080
      HELIX_CONCURRENCY: "4"
      HELIX_POLL_MS: "250"
      HELIX_HEARTBEAT_SECONDS: "10"
      HELIX_LEASE_RENEW_SECONDS: "6"
      RUST_LOG: helix_worker=info
    depends_on:
      - coordinator
    init: true
    restart: unless-stopped
